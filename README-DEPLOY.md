# App compilada — app.synkroai.com

Esta rama contiene SOLO el resultado de `npm run build` del frontend,
lista para servirse tal cual. No editar a mano: se regenera desde
`deploy/app-synkroai` con la URL del backend de Railway integrada.

SiteGround puede apuntar su despliegue por git a esta rama y publicarla
directamente en el subdominio, sin paso de compilacion.
